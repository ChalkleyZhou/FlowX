## 1. 数据模型与加密基础

- [x] 1.1 在 Prisma 新增用户级 AI 凭据表（含 provider、encryptedSecret、keyVersion、时间戳与唯一索引）
- [x] 1.2 新增 `CredentialCryptoService` 并实现基于 `FLOWX_CREDENTIAL_MASTER_KEY` 的加解密
- [x] 1.3 为凭据仓储与加密服务编写单元测试（含失败路径）

## 2. 用户凭据管理 API

- [x] 2.1 新增受会话认证保护的凭据状态查询、设置/更新、删除接口
- [x] 2.2 增加输入校验与响应 DTO，确保接口不返回明文密钥
- [x] 2.3 在前端增加凭据配置入口与状态展示

## 3. 执行链路接入

- [x] 3.1 在工作流上下文中传递发起用户身份到 AI 执行层
- [x] 3.2 在 Cursor 执行器中实现“用户 > 实例级 > 登录态”凭据优先级
- [x] 3.3 增加执行来源记录与错误分类（缺失、解密失败、认证失败）

## 4. 安全与审计

- [x] 4.1 新增凭据更新/删除审计事件
- [x] 4.2 统一日志脱敏策略，禁止输出密钥内容
- [x] 4.3 增加关键错误码与对外错误文案映射

## 5. 验证与文档

- [x] 5.1 增加后端与前端测试覆盖（凭据 CRUD、执行回退逻辑、权限隔离）
- [x] 5.2 更新 Docker 部署与 README 文档，补充用户自有凭据模式
- [x] 5.3 运行 `pnpm --filter flowx-api test`、`pnpm --filter flowx-web test`、`pnpm check`
