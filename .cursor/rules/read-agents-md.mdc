---
description: 任务开始前必须阅读项目 AGENTS.md 及对应子项目指南
alwaysApply: true
---

# 任务开始前阅读 AGENTS.md

在分析需求、改代码、跑命令或回复用户之前，先读取并遵守项目 Agent 指南。

## 必读顺序

1. **仓库根目录** `AGENTS.md` — 每次任务都必须读（项目概览、指令层级、常用命令、高风险区域、提交前检查）。
2. **按改动范围补充**（在动手改对应目录前读取）：
   - 修改 `apps/api/**` → 同时读 `apps/api/AGENTS.md`
   - 修改 `apps/web/**` → 同时读 `apps/web/AGENTS.md`
3. **冲突时**：以更具体、距离改动文件更近的 `AGENTS.md` 为准。

## 执行要求

- 用 Read 工具打开上述文件，不要凭记忆假设命令、目录或约束。
- 子项目规则与根目录规则冲突时，遵守子项目 `AGENTS.md`。
- 未读完相关 `AGENTS.md` 前，不要开始实现、重构或大范围探索性编辑。

## 与现有流程对齐

- 高风险路径（工作流、状态机、Prisma、`apps/web/src/api.ts` 等）以 `AGENTS.md` 中的说明为准，优先补/更新测试再改实现。
- 交付前按 `AGENTS.md` 执行对应验证（如 `pnpm check` 或子项目测试）。
